<form method="post" action="{{ path('app_incidente_delete', {'id': incidente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ incidente.id) }}">
    <button class="btn">Delete</button>
</form>
